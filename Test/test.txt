{
    USHORT a;b;c,

    a = 1_us,
    b = a * 2_us,
    c = 0_us,

    WHILE (c <= 8_us) DO {
        b = b + 5_us - a,
        c++,
    },

    b = 1_us,
    b = b + a,

    IF (b == 2_us) {
        PRINT # VALOR CORRECTO #,
    } END_IF,


}

*{


        CLASS clase {
            USHORT b,
            VOID func() {
              IF (b == 8_us) {
                  PRINT # LLEGUE #,
              } ELSE {
                  PRINT # NO LLEGUE #,
              }
              END_IF,

              RETURN,
            },
        }

    clase obj,
    obj.b = 2_us,

    WHILE (obj.b <= 8_us) DO {
        obj.b = obj.b * 2_us,
        PRINT obj.b,
        obj.func(),
    },


    LONG a,
    a = 3_l * obj.b,

    USHORT p,

    WHILE (obj.b <= 8_us) DO {
        obj.b = obj.b * 2_us,

        p = 9_us,
        p = p + 4_us,

        PRINT obj.b,
        obj.func(),
    },

    USHORT d,
    d = 3_us,

    c = d * obj.b,

    IF (obj.b < 5_us) {
        obj.b = 6_us,
    } END_IF,

    d = d / c,
    LONG e,
    e = 7_us,
    d = e,


}*