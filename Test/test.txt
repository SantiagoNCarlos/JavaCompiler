{
    FLOAT x,
    x = 2.40282347e+38,

    x = x * 1.0,
    PRINT x,
}

*{

    CLASS clase {
        USHORT b,
        VOID func() {
          IF (b == 8_us) {
              PRINT # LLEGUE #,
          } ELSE {
              PRINT # NO LLEGUE #,
          }
          END_IF,

          RETURN,
        },
    }

    clase obj,
    obj.b = 2_us,

    WHILE (obj.b <= 8_us) DO {
        obj.b = obj.b * 2_us,
        PRINT obj.b,
        obj.func(),
    },


}*