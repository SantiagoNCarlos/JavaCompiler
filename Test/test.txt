{
	*{ Test para comprobar operaciones aritmeticas. }*

    USHORT a,
	LONG  b,
	FLOAT c,
	FLOAT d,

	a= 3_us + 1_us,
	b= 8_l,


    a = 3_us / 2_us,

	c = b,

	b = 6_us + 2_l,




    PRINT a,
	PRINT b,
    PRINT c,
    PRINT d,
}

*{


        CLASS clase {
            USHORT b,
            VOID func() {
              IF (b == 8_us) {
                  PRINT # LLEGUE #,
              } ELSE {
                  PRINT # NO LLEGUE #,
              }
              END_IF,

              RETURN,
            },
        }

    clase obj,
    obj.b = 2_us,

    WHILE (obj.b <= 8_us) DO {
        obj.b = obj.b * 2_us,
        PRINT obj.b,
        obj.func(),
    },


    LONG a,
    a = 3_l * obj.b,

    USHORT p,

    WHILE (obj.b <= 8_us) DO {
        obj.b = obj.b * 2_us,

        p = 9_us,
        p = p + 4_us,

        PRINT obj.b,
        obj.func(),
    },

    USHORT d,
    d = 3_us,

    c = d * obj.b,

    IF (obj.b < 5_us) {
        obj.b = 6_us,
    } END_IF,

    d = d / c,
    LONG e,
    e = 7_us,
    d = e,


}*